# 开发记录：学生改名身份对齐规则

## 现象

截图中的学生昵称已经变更，旧学生名单里仍保留真实姓名和历史昵称，直接按截图昵称无法判断“朱圣哲”对应哪一行。

## 根因证据

- 本地学生名单 `Edu/students.2829.json` 中，“朱圣哲”的稳定信息为 `uid=153aa39e164c41d7979fc95a55966369`、`username=SZS1817299200`。
- 通过 OJ 团队成员接口重新拉取 `gid=2829` 后，同一 `uid` / `username` 的最新昵称为“晚安,巴卡玛卡!(狼人请睁眼)”。

## 修复内容

在 `skills/teaching-toolbox-oj-debugging/SKILL.md` 的“学生名单 JSON”小节增加规则：查学生改名后的身份时，以 `uid` / `username` 为稳定标识，把本地名单里的 `real_name` 和 OJ 最新 `nickname` 对齐；不要只按截图当前昵称判断。

## 验证命令和结果

- `test -f skills/teaching-toolbox-oj-debugging/SKILL.md`：确认 skill 文件存在。
- `test -f Edu/docs/开发记录-2026-07-10-学生改名身份对齐规则.md`：确认开发记录存在。
- `rg -n "查学生改名后的身份时|uid.*username.*稳定标识|不要只按截图当前昵称判断" skills/teaching-toolbox-oj-debugging/SKILL.md Edu/docs/开发记录-2026-07-10-学生改名身份对齐规则.md`：确认规则已写入 skill 和开发记录。

## 未完成或需要用户处理的事项

本次只更新规则，没有改写 `Edu/students.2829.json` 中的历史昵称数据。
